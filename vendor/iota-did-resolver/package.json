{
  "name": "@thisdid/iota-did-resolver",
  "version": "1.0.0",
  "description": "did:iota resolver for the DIF did-resolver interface — Identity Move objects read straight from IOTA Rebased fullnode JSON-RPC and unpacked offline.",
  "type": "module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "files": [
    "lib",
    "LICENSE"
  ],
  "sideEffects": false,
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "test": "vitest run",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\""
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "did-resolver": ">=4.1.0"
  },
  "dependencies": {
    "did-resolver": "^4.1.0"
  }
}
