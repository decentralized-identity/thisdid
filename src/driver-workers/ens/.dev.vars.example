# Alchemy token only — the base URL lives as a public var in wrangler.jsonc and
# the code appends this key after its trailing `/`. Copy to .dev.vars for
# local development; set as a Worker secret for deploys.
ALCHEMY_API_KEY=replace-me
