# Alchemy token only — the base URLs live as public vars in wrangler.jsonc and
# the code appends this key after their trailing `/`. Copy to .dev.vars for
# local development; set as a Worker secret for deploys.
ALCHEMY_API_KEY=replace-me
